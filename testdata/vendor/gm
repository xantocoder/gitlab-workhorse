#!/usr/bin/env sh
set -e

cmd=$1
resize=$2
resize_opts=$3
infile=$4
outfile=$5

wd=`pwd`

docker pull irineutfilho/graphicsmagick &> /dev/null
docker run --rm -i -v $wd:/wd -w /wd irineutfilho/graphicsmagick gm $@
